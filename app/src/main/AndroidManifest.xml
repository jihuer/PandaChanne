<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a12710.pandachannel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".global.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/logo_ipnda"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GungunPlay"></activity>

        <activity android:name=".activity.Panda_Report_InfoActivity" />
        <activity android:name=".activity.personal_info.PersonInfoActivity" />
        <activity android:name=".activity.personal_info.LoginActivity" />
        <activity android:name=".activity.personal_info.RegisterActivity" />
        <activity android:name=".activity.personal_info.SettingActivity" />
        <activity android:name=".activity.personal_info.ShouCangActivity" />
        <activity android:name=".activity.personal_info.HistoryActivity"></activity>

    </application>

</manifest>